#include "monty.h"

/**
 * mount - chooses a mode.
 * @stuck: The pointer
 * @Number: It counts.
 * Return: 0 if (success)
 */
void mount(stack_h **stuck, unsigned int Number)
{
	(void) stuck;
	(void) Number;
	queues = 's';
}
